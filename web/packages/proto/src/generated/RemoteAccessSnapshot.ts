// DO NOT EDIT — generated by wiregen (`cargo run -p wiregen`; gate: `-- --check`).

import type { PairedSession } from "./PairedSession";
import type { RemoteAccessStatus } from "./RemoteAccessStatus";

/**
 * The `GetRemoteAccess` snapshot; also the reply of `SetRemoteAccess` and
 * `RevokePairingSession`, so a caller always lands on fresh state.
 */
export type RemoteAccessSnapshot = { status: RemoteAccessStatus, sessions: Array<PairedSession>, };

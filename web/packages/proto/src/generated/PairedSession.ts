// DO NOT EDIT — generated by wiregen (`cargo run -p wiregen`; gate: `-- --check`).

/**
 * A paired client session — the credential a paired client holds for an
 * engine (CONTEXT.md). Does not expire; lives until revoked.
 */
export type PairedSession = { id: string, label: string, createdAt: number, lastSeen: number, revokedAt: number | null, };

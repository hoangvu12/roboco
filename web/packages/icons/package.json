{
  "name": "@roboco/icons",
  "version": "0.0.0",
  "private": true,
  "description": "The Roboco control-icon set for the web client — the same SVG assets the desktop embeds (crates/ui/assets/icons), generated into one module plus a React <Icon> that tints with currentColor. Regenerate with `pnpm regenerate`; a CI gate fails when the generated module drifts from the Rust assets.",
  "type": "module",
  "exports": {
    ".": "./src/index.tsx",
    "./generated": "./src/generated/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "regenerate": "node scripts/generate.mjs",
    "check": "node scripts/generate.mjs --check"
  },
  "packageManager": "pnpm@11.17.0",
  "peerDependencies": {
    "react": "^19.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.5",
    "typescript": "5.9.3"
  }
}

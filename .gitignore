/target
**/*.rs.bk
# Local-only scratch space — holds signing/notary credentials, never commit.
/ignore/
*.pdb
.DS_Store
.env
.env.local
node_modules
.wrangler
build/
# Staged web bundle for rust-embed — written by crates/engine/build.rs.
crates/engine/web-staging/
